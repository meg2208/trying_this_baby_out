<%= form_for @dog, :html => { :class => 'form-horizontal form-model' } do |f| %>
  <div class="form-group">
    <%= f.label :name, :class => 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.text_field :name, :class => 'text-field form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :breed, :class => 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.text_field :breed, :class => 'text-field form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :bday, :class => 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.datetime_select :bday, :class => 'text-field form-control' %>
    </div>
  </div>

  <div class="form-group">
    <div class='col-md-offset-2 col-md-10'>
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                  dogs_path, :class => 'btn btn-default' %>
    </div>
  </div>
<% end %>
